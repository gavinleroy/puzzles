#include <cstdio>
#include <algorithm>
#include <cmath>

typedef double db;

using namespace std;

struct point{
	db x;
	db y;
};

int main(void){
	point p1, p2;
	db r;
	scanf("%lf %lf %lf %lf %lf", &p1.x, &p1.y, &p2.x, &p2.y, &r);
	return 0;
}
